server:
  port: 8080
spring:
  application:
    name: tfs-feeder-service
  datasource:
    url: "jdbc:postgresql://localhost:3306/tfs-feeder-service"
    username: "username"
    password: "password"
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true