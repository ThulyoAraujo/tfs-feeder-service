server:
  port: 8080
spring:
  application:
    name: tfs-feeder-service
  datasource:
    url: "jdbc:postgresql://localhost:5432/tfs_feeder_service"
    username: "username"
    password: "PostTeste123"
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true